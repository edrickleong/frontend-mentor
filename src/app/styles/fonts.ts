import { Hanken_Grotesk, Outfit, Overpass } from "next/font/google"

export const outfit = Outfit({
  subsets: ["latin-ext"],
})

export const overpass = Overpass({
  subsets: ["latin-ext"],
})

export const hankenGrotesk = Hanken_Grotesk({
  subsets: ["latin-ext"],
})
